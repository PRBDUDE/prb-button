import { Component } from '@angular/core';
import {PlainButton} from '@prb/buttons';

@Component({
  selector: 'prb-plain-demo',
  imports: [
    PlainButton
  ],
  templateUrl: './plain-demo.html',
  styleUrl: './plain-demo.scss',
})
export class PlainDemo {

}
